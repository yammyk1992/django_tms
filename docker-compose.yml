services:
  web-app:
    build:
      context: .
    command: ["python", "manage.py", "runserver", "0.0.0.0:8000"]
    volumes:
      - ./my_django/:/PycharmProjects/django_tms/
    ports:
      - 8000:8000


